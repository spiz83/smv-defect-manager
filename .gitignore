node_modules/
package-lock.json
package.json
_check.js

.vercel

# Claude Code local settings — can contain API tokens; never commit
.claude/settings.local.json
.claude/settings.local.*.json

# Local env files
.env
.env.*
tests/artifacts/

# Real customer plan sets used to check sheet naming — never committed:
# they carry a client name and address in the title block, and everything in
# this repo is deployed to the live site. tests/jobplans.mjs section H runs
# only if one happens to be present locally.
tests/fixtures-plan-*.pdf
